name: Test & Build

on:
  pull_request:
    branches: [ master, 6_2 ]

jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Select Xcode 
        run: sudo xcode-select -switch /Applications/Xcode_12.app
      - name: Test & Build
        run: xcodebuild clean test -workspace neon.xcworkspace -scheme oxygen -destination "platform=iOS Simulator,name=iPhone 11 Pro"
